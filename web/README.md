## 一、AI 对话功能

  通过 `!ai <内容>` 命令与 DeepSeek Chat 对话，AI 能识别播放指令并自动控制机器人。

  ### 配置

  在网页设置 → AI 设置中填入 DeepSeek API Key 即可启用。

  ### 命令

  - `!ai <问题>` — 向 AI 提问，AI 可以自动执行播放命令

  ### 注意事项

  - 需要 DeepSeek API Key（从 platform.deepseek.com 获取）
  - AI 回答会被截断到 200 字以内
